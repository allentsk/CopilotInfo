# Docker directory

Doccker is run outside the construct of my `MidiRouter` repository.

| FileName                   | Description            |
| -----------                | ---------------------- |
| Dockerfile                 | self explanitory       |
| readme.md                  | this file              |
| scripts                    | scripts to build the docker image etc              |
| scripts/rundocker.zsh      | execution of the docker image when I want to do a docker mounted image  |
| scripts/setghworkspace.zsh | Used to set enviornment variable before rundocker  |
| scripts/builddocker.zsh    | builds the docker image                            |
